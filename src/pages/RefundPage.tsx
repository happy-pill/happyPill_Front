import InfoPage from '../components/InfoPage';
import { refundPageData } from '../data/refundContent';

const RefundPage = () => {
  return <InfoPage {...refundPageData} />;
};

export default RefundPage;